create database called regis

run the following query under SQL in your db

CREATE TABLE `users` (
    `id` int(11) NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `username` varchar(100) NOT NULL,
    `email` varchar(100) NOT NULL,
    `password` varchar(100) NOT NULL
)

CREATE TABLE Recipe (
   rid MEDIUMINT not null AUTO_INCREMENT,   
   username  varchar(40),
   rtitle varchar(100)not null,
   rServNum int default null,
   rCookTime varchar default null,  /*for minutes*/
   rSteps  varchar(1200),
   rcategory varchar(255) not null,
   author varchar(255) not null,
   primary key (rid), 
   foreign key (username) references Users(username)
)

CREATE TABLE Ingredient (
   iid MEDIUMINT not null AUTO_INCREMENT,
   rid MEDIUMINT  not null,
   Iname varchar(100),
   Iqty float,
   Unit  varchar(100),
   primary key (iid),
   foreign key (rid) references Recipe(rid),
   foreign key (Unit) references UnitConversion(Unit)

)

CREATE TABLE Bookmark(   
   booNum  MEDIUMINT not null AUTO_INCREMENT,  
   rid     MEDIUMINT  not null, 
   bname    varchar(40) not null,
   primary key (booNum),   
   foreign key (rid) references Recipe(rid),   
   foreign key (bname) references Users(username)
)

copy the folder and paste it in C:\xampp\htdocs\folder_name

start xamp and turn on apache and mysql server

go to browser and redireect to localhost/folder_name
